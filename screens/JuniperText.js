import React from 'react';
import { Text } from 'react-native';

const JuniterText = ({content, style}) => {

    return(
    <Text style={style}>{content}</Text>
    );
}

export default JuniterText;
